# Dream Spot Global — dependencies for cPanel shared hosting (Python 3.12)
# Install with:  pip install -r requirements-cpanel.txt
Django==5.1.15
Pillow==11.0.0
whitenoise==6.8.2
PyMySQL==1.1.1          # pure-Python MySQL driver, no compiler needed
python-dotenv==1.0.1
